class Base 
{
    new(a, b)
    {
        print("Base.new(" + a + ", " + b + ")")
    }
}

class Derived : Base 
{
    new()
    {
        print("Derived.new()")
        super("a", "b")
    }
}

Derived()
// expect: Derived.new()
// expect: Base.new(a, b)
