class Base 
{
    constructor(a) 
    {
        this.a = a
    }
}

class Derived : Base 
{
    constructor(a, b)
    {
        super(a)
        this.b = b
    }
}

var derived = Derived("a", "b")
print(derived.a) // expect: a
print(derived.b) // expect: b
