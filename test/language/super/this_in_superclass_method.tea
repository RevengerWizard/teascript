class Base 
{
    new(a) 
    {
        self.a = a
    }
}

class Derived : Base 
{
    new(a, b)
    {
        super(a)
        self.b = b
    }
}

var derived = Derived.new("a", "b")
print(derived.a) // expect: a
print(derived.b) // expect: b
