class Foo 
{
    new() {}
    getClosure() 
    {
        function closure() 
        {
            return self.toString()
        }
        return closure
    }

    toString() { return "Foo" }
}

var closure = Foo.new().getClosure()
print(closure()) // expect: Foo
