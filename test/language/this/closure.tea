class Foo 
{
    getClosure() 
    {
        function closure() 
        {
            return this.toString()
        }
        return closure
    }

    toString() { return "Foo" }
}

var closure = Foo.new().getClosure()
print(closure()) // expect: Foo
