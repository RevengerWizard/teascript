import time

function fib(n)
{
    if(n < 2) return n

    return fib(n - 2) + fib(n - 1)
}

var start = time.clock()

for(var i in 1..5)
{
    print(fib(28))
}

print("elapsed: " + string(time.clock() - start))