import random

var face = 1..6

var roll = "yes"

while roll == "yes" or roll == "y"
{
    roll = input("Roll the dices? ")

    print("Rolling The dices...")
    print("The values are :")
    
    print(random.range(face))
    print(random.range(face))
    print()
}