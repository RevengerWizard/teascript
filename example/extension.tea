class Foo
{
    new(x, y)
    {
        self.x = x
        self.y = y
    }
}

function Foo:say()
{
    self.said = "hello hello"
    print(self, self.said)
}

var foo = Foo.new(12, 32)
foo.say()
print(foo.said)