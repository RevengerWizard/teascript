class Timer
{
    new(time)
    {
        self.total = time
        self.left = 0
    }

    function start()
    {
        self.left = self.total
    }

    function step(delta)
    {
        self.left -= delta
    }

    function isover()
    {
        return self.left < 0
    }
}

const timer = Timer.new(15)
timer.start()

while
{
    print(timer.left)
    timer.step(1)

    if timer.isover()
    {
        break
    }
}